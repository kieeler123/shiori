🧾 📌 리스트 형식
🇯🇵 日本語

詳細ページの下部に「同一作者の投稿」セクションを追加

最新順 / 人気順（view_count）で切り替え可能に実装

表示件数は5件に制限

sort状態をuseStateで管理し、ボタンでトグル

dbListRelatedLogsを作成し、user_idベースでデータ取得

created_atではなくdisplay_date基準でソート하도록修正

初期実装では人気順でデータが表示されない問題が発生

ログ（raw / visible）を分離して原因を特定

DBデータは取得されていたが、フロントフィルタで全て除外されていた

related logsではメインリスト用フィルタが強すぎることを確認

フィルタ戦略を用途ごとに分ける必要性を理解

🇺🇸 English

Added “author’s other posts” section under log detail page

Implemented sorting toggle: recent / views

Limited results to 5 items

Managed sort state using useState

Created dbListRelatedLogs with user_id filtering

Switched sorting from created_at to display_date

Encountered issue where no data appeared in views sort

Debugged using separate logs for raw vs filtered data

Found that data existed but was removed by frontend filtering

Identified that main list filter was too strict for related logs

Learned importance of context-based filtering strategy

🇰🇷 한국어

상세페이지 하단에 작성자 글 섹션 추가

최신순 / 인기순(view_count) 정렬 기능 구현

5개 제한으로 목록 구성

useState로 sort 상태 관리 및 토글 버튼 구현

dbListRelatedLogs 함수 생성 (user_id 기준 조회)

created_at → display_date 기준으로 정렬 수정

인기순에서 데이터가 안 보이는 문제 발생

raw / visible 로그 분리로 원인 추적

DB 데이터는 존재했지만 프론트 필터에서 전부 제거됨

메인 리스트 필터가 related logs에는 너무 강하다는 점 확인

필터 전략을 UI 성격에 따라 다르게 설계해야 한다는 점 이해