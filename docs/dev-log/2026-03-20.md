🧾 1️⃣ 리스트 형식
🇯🇵 日本語

テキスト内に [[table:1]] のようなトークンを挿入する方式を設計した

テーブルデータを table_data JSON構造として分離して管理した

TableEditor コンポーネントを作成し、行・列の追加/削除機能を実装した

カーソル位置にテーブルを挿入する機能を実装した

Textarea を forwardRef 対応にしてカーソル制御を可能にした

tableId は固定（1）にして、編集時は同じテーブルを更新する構造にした

表示時には content をパースして該当位置にテーブルをレンダリングした

UIの全テキストを i18n 対応できるように設計した

🇺🇸 English

Designed a token-based system using [[table:1]] inside content

Separated table data into a structured table_data JSON format

Built a TableEditor component with row/column add & remove functionality

Implemented insertion of tables at the cursor position

Enabled cursor control by converting Textarea to forwardRef

Fixed tableId to "1" and updated the same table instead of creating new ones

Parsed content and rendered tables dynamically in the detail page

Prepared all UI strings for i18n support

🇰🇷 한국어

content 안에 [[table:1]] 토큰을 넣는 구조 설계

표 데이터를 table_data JSON으로 분리 관리

TableEditor 컴포넌트로 행/열 추가/삭제 기능 구현

커서 위치에 표를 삽입하는 기능 구현

Textarea를 forwardRef로 변경하여 커서 제어 가능하게 만듦

tableId를 1로 고정하고 수정 시 같은 표를 업데이트하도록 설계

상세페이지에서 content 파싱 후 해당 위치에 표 렌더링

UI 문자열을 i18n 구조로 분리