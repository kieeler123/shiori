🇯🇵 日本語
📅 今日やったこと

Supabase OAuth（Googleログイン）のリダイレクト問題をデバッグ

redirectTo が正しく設定されているか検証

skipBrowserRedirect: true を使って OAuth URL を直接確認

window.location.assign / replace のトレースで強制リダイレクトの有無を検証

next パラメータの正規化処理を追加

最終原因を特定
→ Redirect URLs に https://localhost
 を登録していたため、http://localhost
 と一致せず fallback が発生

🧠 技術的理解

Supabase は Redirect URL を「完全一致」で検証する

一致しない場合、Site URL にフォールバックする

OAuth のプロトコル（http/https）は完全一致が必須

navigate() はドメイン移動を起こさない

🇺🇸 English
📅 What I Did Today

Debugged Supabase OAuth (Google login) redirect issue

Verified correctness of redirectTo

Used skipBrowserRedirect: true to inspect OAuth URL

Traced window.location.assign / replace

Added normalization for next parameter

Identified root cause:
→ Redirect URL registered as https://localhost, but app was running on http://localhost

🧠 Technical Understanding

Supabase validates redirect URLs with strict matching

Protocol (http/https) must match exactly

If not matched, Supabase falls back to Site URL

React Router navigate() does not change domain

🇰🇷 한국어
📅 오늘 한 일

Supabase OAuth 로그인 리다이렉트 문제 디버깅

redirectTo 설정 확인

skipBrowserRedirect 옵션으로 OAuth URL 직접 확인

window.location 강제 이동 여부 추적

next 값 정규화 로직 점검

최종 원인 발견
→ Redirect URLs에 https://localhost를
 등록해두어 http와 불일치 발생

🧠 기술적 이해

Supabase Redirect URL은 문자열 완전 일치 검사

프로토콜(http/https)까지 정확히 같아야 함

불일치 시 Site URL(배포)로 fallback

React Router navigate는 도메인 이동을 발생시키지 않음

- Supabase redirect https 문제 해결
- OAuth callback 안정화
- Route 보호 구조 정리
- Header 레이어 정리
- Button 토큰화
- List / Editor 레이어 분리 시작
- Dark theme text scale 정리